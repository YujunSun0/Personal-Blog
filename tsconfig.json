{
  "compilerOptions": {
    // TypeScript가 코드를 JS로 “변환한다면” 어느 정도 문법까지 목표로 할지
    "target": "ES2017",
    // 타입 정의(내장 API)를 어떤 걸 쓸지.
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    // JS 파일(.js/.jsx)도 프로젝트에 섞여 있어도 허용.
    "allowJs": true,
    // node_modules 안의 타입 정의(d.ts)까지 “엄격히 검사” 할지
    "skipLibCheck": true,
    // TypeScript 엄격 모드 풀세트  (null/undefined, 타입 추론)
    "strict": true,
    // TypeScript가 JS 파일을 출력하지 않게 함.
    "noEmit": true,
    // CommonJS(require) 기반 라이브러리를 ES import로 편하게 쓰게 해주는 호환 옵션.
    "esModuleInterop": true,
    // 모듈 시스템을 최신 ES 모듈 기준으로 둠.
    "module": "esnext",
    "moduleResolution": "bundler",
    // import data from "./data.json" 같은 JSON import 허용.
    "resolveJsonModule": true,
    // 각 파일을 독립적으로 컴파일 가능해야 한다는 제약.
    "isolatedModules": true,
    // JSX를 TS가 변환하지 않고 그대로 유지.
    // Next.js/SWC가 JSX 변환을 담당하기 때문에 이처럼 설정
    "jsx": "react-jsx",
    // 타입체크 캐시를 사용해서 다음 실행을 빠르게 함.
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    // import Button from "@/components/Button"; 이런 식으로 사용 가능 (실무에서 상대경로 지옥(../../..) 피하려고)
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts"
  ],
  "exclude": [
    "node_modules"
  ]
}
